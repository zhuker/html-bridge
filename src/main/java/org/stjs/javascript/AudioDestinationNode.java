package org.stjs.javascript;

public class AudioDestinationNode extends AudioNode {
    /**
     * Is an unsigned long defining the maximum amount of channels that the
     * physical device can handle.
     */
    public long maxChannelCount;
}
